import { HeaderText } from '@gamepark/react-game'

/**
 * Crossing 8 points, and then 20 (see {@link BonusTokenRule}).
 *
 * The tokens are what is chosen, so each of them wears the offer to spend it, with what it pays
 * written beside it (see `ChooseBonusButton`): the bar only says what to do.
 */
export const BonusTokenHeader = () => <HeaderText code="bonus" />
