/**
 * Two faces, and no more.
 *
 * Grenze is cut after medieval manuscript hands, which is what the cards are lettered in: the swashed
 * initial of "Guilde des voyageurs" and the drop caps of the rulebook belong to the same family. It
 * carries the names, the headings and anything the player clicks.
 *
 * Nunito stays for running text, as in the org's other Game Park adaptations (see ../aurealis): a
 * card's rules have to be read quickly and in six languages, and a manuscript hand is the wrong tool
 * for that.
 */
export const fontDisplay = `'Grenze', Georgia, serif`
export const fontBody = `'Nunito', 'Segoe UI', sans-serif`
