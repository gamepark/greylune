/**
 * Each year runs through 4 seasons. Players move through them at their own pace: one may still be
 * in Spring while another has already reached Autumn.
 */
export enum Season {
  Winter = 1,
  Spring,
  Summer,
  Autumn
}
